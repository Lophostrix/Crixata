{
  "name": "@crixata/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build && npm run copy-manifest-and-icons",
    "typecheck": "tsc --noEmit",
    "copy-manifest-and-icons": "node -e \"const fs = require('fs'); if (!fs.existsSync('dist')) fs.mkdirSync('dist'); fs.cpSync('icons', 'dist/icons', {recursive: true}); fs.copyFileSync('manifest.json', 'dist/manifest.json');\"",
    "test": "node test/verify.mjs"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "typescript": "^5.6.2",
    "vite": "^6.0.3"
  }
}
